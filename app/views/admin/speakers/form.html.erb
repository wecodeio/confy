<%= simple_form_for [:admin, @speaker] do |f| %>

    <%= render :partial => 'shared/errors', locals: { f: f } %>

    <%= f.input :name %>
    <%= f.input :slug %>
    <%= f.input :avatar %>

    <%= f.button :submit %>

<% end %>